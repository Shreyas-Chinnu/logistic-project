module.exports = {
  eslint: {
    ignoreDuringBuilds: true, // Skip ESLint checks during build
  },
};
module.exports = {
  eslint: {
    ignoreDuringBuilds: true, // Skip ESLint checks during build
    lintOnSave: false, // Disable ESLint checks on save
    fixOnSave: false, // Disable auto-fixing on save
  },
};